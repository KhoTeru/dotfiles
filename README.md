# Tips
- Add 'AddKeysToAgent' to '~/.ssh/config'.
- Add the following services after the installation:
    - [ ] ssh-agent (user) -> Also `ssh-add` the key to the agent.

# TODO

- [ ] Add scripts for automation depending on the machine type (native, VM)
